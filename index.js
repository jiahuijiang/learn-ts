const greeting = "Hello"
const place = "World"

// text

console.log('%s %s 👋', greeting, place);
console.log("%s %s 👋", greeting, place);
// easier to read
console.log(`${greeting} ${place}`);
